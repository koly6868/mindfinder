version: "3.0"

services:
    mindfiner:
        image: "mindfiner"
        build: 
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/app
        env_file:
            - .env
        depends_on: 
            - neobase
            - postgres
        ports:
            - "8000:8000"
    
    neobase:
        image : amd64/neo4j:3.5
        volumes:
            - ./data/neobase:/data
        env_file:
            - .env
        ports:
            - "7687:7687"
            - "7474:7474"
    
    postgres:
        image: postgres
        env_file:
            - .env
        ports:
            - "5432:5432"
        volumes:
            - ./data/db:/var/lib/postgresql/data
    
    makemigrations:
        image: "mindfiner"
        build: 
            context: .
            dockerfile: Dockerfile
        command: "python manage.py makemigrations"
        volumes:
            - .:/app
        env_file:
            - .env
        depends_on: 
            - neobase
            - postgres
    
    migrate:
        image: "mindfiner"
        build: 
            context: .
            dockerfile: Dockerfile
        command: "python manage.py migrate"
        volumes:
            - .:/app
        env_file:
            - .env
        depends_on: 
            - neobase
            - postgres

    install_labels:
        image: "mindfiner"
        build: 
            context: .
            dockerfile: Dockerfile
        command: "python manage.py install_labels"
        volumes:
            - .:/app
        env_file:
            - .env
        depends_on: 
            - neobase
            - postgres
        
    
    
